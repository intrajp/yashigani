# Makefile.am - use automake to generate Makefile.in

CC = gcc
AM_CXFLAGS = $(INIT_CFLAGS)
AM_LDFLAGS = -lcrypto 

yashigani_SOURCES = main.c yashigani.c common.c line_data.c

bin_PROGRAMS = yashigani 

all:

clean-local:

